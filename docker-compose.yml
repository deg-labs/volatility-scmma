version: '3.8'

services:
  fetcher:
    build:
      context: fetcher
      dockerfile: Dockerfile
    container_name: cmma-fetcher
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    env_file:
      - .env
    restart: always
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "2"

  api:
    build:
      context: api
      dockerfile: Dockerfile
    container_name: cmma-api
    depends_on:
      - fetcher
    volumes:
      - ./data:/app/data:ro
    restart: always
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "2"

  nginx:
    build:
      context: nginx
      dockerfile: Dockerfile
    container_name: cmma-nginx
    depends_on:
      - api
    ports:
      - "8001:80"
    restart: always
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "2"

